type WelcomeScreen {
  id: Int!
  title: String!
  text: String
  imageUrl: String!
  language: LanguageEnum!
  createdAt: DateTime!
  updatedAt: DateTime
}

input CreateWelcomeScreenInput {
  title: String
  text: String!
}

input UpdateWelcomeScreenInput {
  id: Int!
  text: String
  title: String
}

type Query {
  welcomeScreen(language: LanguageEnum!, search: String): [WelcomeScreen]!
}

type Mutation {
  createWelcomeScreen(
    createWelcomeScreenInput: CreateWelcomeScreenInput!
    language: LanguageEnum!
    image: Upload!
  ): WelcomeScreen!
  updateWelcomeScreen(
    updateWelcomeScreenInput: UpdateWelcomeScreenInput!
    language: LanguageEnum
    image: Upload
  ): WelcomeScreen!
  removeWelcomeScreen(id: Int!): WelcomeScreen
}
